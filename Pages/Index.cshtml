@page
@model IndexModel
@{
    ViewData["Title"] = "Accueil";
}

<style>
/* Styles spécifiques à la page d''accueil */
.hero-section { 
    padding: 4.5rem 0; 
    min-height: calc(100vh - 200px);
}

.gradient-text { 
    background: linear-gradient(90deg, #a78bfa, #60a5fa); 
    -webkit-background-clip: text; 
    -webkit-text-fill-color: transparent;
    background-clip: text; 
    font-weight: 700;
}

.feature-list { 
    margin-top: 1.5rem; 
    list-style: none; 
    padding-left: 0; 
    color: #cfe7ff; 
}

.feature-list li { 
    position: relative; 
    padding-left: 24px; 
    margin-bottom: 10px; 
    font-size: 1.05rem;
}

.feature-list li::before { 
    content: "✓"; 
    position: absolute; 
    left: 0; 
    color: #7dd3fc; 
    font-weight: bold;
    font-size: 1.1rem;
}

.mockup-card { 
    background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.02)); 
    border-radius: 16px; 
    border: 1px solid rgba(255,255,255,0.1);
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
}

.mockup-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.3);
}

.dot-group .dot { 
    width: 10px; 
    height: 10px; 
    display: inline-block; 
    background: #e6eef8; 
    border-radius: 50%; 
    margin-right: 8px; 
    opacity: 0.2; 
}

.chat-preview { 
    min-height: 160px; 
    border-radius: 12px; 
    padding: 16px; 
    display: flex; 
    flex-direction: column; 
    gap: 12px; 
    background: rgba(0,0,0,0.2);
}

.chat-message { 
    padding: 12px 16px; 
    border-radius: 16px; 
    display: inline-block; 
    max-width: 85%; 
    font-size: 0.95rem;
    line-height: 1.4;
}

.chat-message.bot { 
    background: linear-gradient(90deg, #0ea5e9, #7dd3fc); 
    color: #001428; 
    align-self: flex-start; 
    font-weight: 500;
}

.chat-message.user { 
    background: rgba(255,255,255,0.08); 
    color: #e6eef8; 
    align-self: flex-end; 
    border: 1px solid rgba(255,255,255,0.1);
}

.pulse { 
    animation: pulse-anim 0.6s ease; 
}

@@keyframes pulse-anim { 
    0% { transform: scale(1); } 
    50% { transform: scale(1.02); } 
    100% { transform: scale(1); } 
}

/* Responsive design amélioré */
@@media (max-width: 767px) {
    .hero-section { 
        padding: 2rem 0; 
        min-height: calc(100vh - 150px);
    }
    .mockup-card { 
        margin-top: 2rem; 
    }
    .display-5 {
        font-size: 1.8rem;
        line-height: 1.2;
    }
    .chat-message {
        max-width: 95%;
        font-size: 0.9rem;
    }
    .feature-list {
        margin-top: 1rem;
    }
    .feature-list li {
        font-size: 1rem;
    }
}

@@media (max-width: 576px) {
    .hero-section {
        padding: 1.5rem 0;
    }
    .display-5 {
        font-size: 1.5rem;
    }
    .lead {
        font-size: 1rem;
    }
    .btn-lg {
        padding: 0.75rem 1.5rem;
        font-size: 1rem;
    }
    .d-flex.gap-2 {
        flex-direction: column;
        gap: 0.75rem !important;
    }
    .d-flex.gap-2 .btn {
        width: 100%;
    }
}
</style>

<section class="hero-section d-flex align-items-center">
    <div class="container">
        <div class="row align-items-center gy-4">
            <div class="col-lg-6">
                <h1 class="display-5 fw-bold gradient-text mb-4">Créez un chatbot AI personnalisé pour votre site</h1>
                <p class="lead mb-4" style="color:rgba(235,244,255,0.85)">
                    Importez vos documents, fournissez les infos de l''entreprise - notre AI génère le widget prêt à intégrer.
                </p>

                <div class="d-flex gap-2 flex-wrap mb-4">
                    <a asp-page="/Create" class="btn btn-lg btn-primary rounded-pill px-4">Commencer</a>
                    <a asp-page="/Docs" class="btn btn-lg btn-outline-light rounded-pill px-4">Voir la démo</a>
                </div>

                <ul class="feature-list">
                    <li>Pré-training à partir de vos documents (FAQ, politique, guide)</li>
                    <li>Widget léger, intégration 1-ligne</li>
                    <li>Respect de la confidentialité &amp; export</li>
                </ul>
            </div>

            <div class="col-lg-6">
                <div class="mockup-card p-4 shadow-lg">
                    <div class="mockup-header d-flex justify-content-between align-items-center mb-3">
                        <div class="dot-group">
                            <span class="dot"></span><span class="dot"></span><span class="dot"></span>
                        </div>
                        <small class="text-muted" style="color:rgba(235,244,255,0.6)">Aperçu temps réel</small>
                    </div>

                    <div class="chat-preview">
                        <div class="chat-message bot">
                            Bonjour 👋 - je suis le chatbot de NexGen. Comment puis-je vous aider aujourd''hui ?
                        </div>
                        <div class="chat-message user">Quels sont vos horaires ?</div>
                        <div class="chat-message bot">
                            Nous sommes ouverts du lundi au samedi, 8h-17h. Avez-vous d''autres questions ?
                        </div>
                    </div>

                    <div class="mt-3 text-end">
                        <button class="btn btn-sm btn-outline-light rounded-pill" id="showCodeBtn">
                            Voir le code d''intégration
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Modal markup injecté par JS -->
<div id="integrationModalPlaceholder"></div>



